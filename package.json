{
  "name": "shreedurga-interior",
  "version": "1.0.0",
  "description": "Shree Durga Interior — Full Stack Website",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "npx serve frontend -p 3000 -s",
    "install:all": "npm install && cd backend && npm install",
    "start": "cd backend && npm start",
    "deploy:vercel": "vercel --prod",
    "deploy:vps": "ssh $VPS_USER@$VPS_HOST 'cd /var/www/shreedurga-interior && git pull && cd backend && npm install --production && pm2 reload shreedurga-api'",
    "build": "echo 'Static site — no build step needed'",
    "lint": "echo 'Add eslint config for linting'"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "serve": "^14.2.1",
    "vercel": "^32.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/YOUR_USERNAME/shreedurga-interior.git"
  },
  "keywords": [
    "interior-design",
    "assam",
    "duliajan",
    "fullstack",
    "express",
    "supabase"
  ],
  "license": "UNLICENSED"
}
